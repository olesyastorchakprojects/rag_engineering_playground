apiVersion: 1

providers:
  - name: rag-runtime-dashboards
    folder: rag_runtime
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

