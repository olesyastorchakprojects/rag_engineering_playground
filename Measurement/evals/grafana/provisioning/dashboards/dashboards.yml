apiVersion: 1

providers:
  - name: eval-dashboards
    folder: evals
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/eval-dashboards
