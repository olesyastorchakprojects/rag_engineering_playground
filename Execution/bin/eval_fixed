#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV_PYTHON="$(cd "$SCRIPT_DIR/../.." && pwd)/.venv/bin/python3"
exec "$VENV_PYTHON" "$SCRIPT_DIR/run_stack.py" eval-engine --scenario fixed "$@"
